{
  "name": "global-price-map",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "구독 서비스 국가별 최저가 비교 — GitHub Pages + Actions로만 운영되는 정적 사이트",
  "engines": {
    "node": ">=20.3.0"
  },
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "validate": "tsx scripts/validate-data.ts",
    "fetch-rates": "tsx scripts/fetch-rates.ts",
    "generate": "tsx scripts/generate-data.ts && tsx scripts/generate-seo.ts"
  },
  "dependencies": {
    "astro": "^7.0.3",
    "pretendard": "^1.3.9"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  }
}
